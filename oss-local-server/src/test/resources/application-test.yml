spring:
  datasource.url: jdbc:h2:mem:oss_local
  jpa.database-platform: org.hibernate.dialect.H2Dialect

# 日志配置
logging:
  level:
    ROOT: INFO
    "[cn.springseed]": DEBUG    
    "[org.springframework.security]": DEBUG
    "[org.springframework.web]": DEBUG  